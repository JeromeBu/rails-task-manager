<h1>Index</h1>

  <%= link_to "New task", new_task_path %>
  <hr>
  <ul>

  <% @tasks.each do |task| %>

  <li><%= link_to "Detail", task_path(task) %> - <%= task.name%> ------- fait: <%= task.done%> - <%= link_to "Edit", edit_task_path(task) %> - <%= link_to "Delete", task_path(task), method: :delete,data: { confirm: "Are you sure you want to delete this task ?" } %></li>

  <% end %>
</ul>
